# Path to the tracker-state.json file exported by BizHawk
# Default: looks in the ironmon_tracker/ folder relative to this project's parent
STATE_FILE_PATH=../ironmon_tracker/tracker-state.json

# No API key needed! The chat uses the Claude Agent SDK which
# authenticates through your existing Claude subscription (Pro/Max).
